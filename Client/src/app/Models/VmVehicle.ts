import { Vehicle } from './Vehicle';
import { VehicleDocument } from './VehicleDocument';
export class VmVehicle {
    Vehicle: Vehicle;
    lstVehicleDocument: VehicleDocument[] = new Array<VehicleDocument>();
}